# 임베드(SetParent) 재검토 — 실측 기록 (2026-07-29)

`hwp_dock.py` 머리말은 임베드를 "검토 끝에 버렸다"고 적어 두었지만 **근거가
추측이었다.** 이번에 `spikes/embed_spike.py` 로 전용 한글 인스턴스를 띄워
직접 재봤다. 선생님이 열어 둔 한글에는 손대지 않았다(DispatchEx 로 새 프로세스).

## 잰 것과 결과

| 항목 | 결과 |
|---|---|
| SetParent 자체 | ✅ 된다. `GetParent` 가 우리 Tk 프레임 핸들로 바뀐다 |
| 제목줄 떼고 판에 꽉 채우기 | ✅ 된다 (`WS_CHILD` + 스타일 제거 + `SetWindowPos`) |
| 임베드 중 COM | ✅ 멀쩡하다. `InsertText` 0.02~0.14초 |
| 임베드 중 우리 이벤트 루프 | ✅ 계속 돈다 (심장박동 카운터 증가) |
| 정상 해제 | ✅ 원래 창으로 돌아오고 COM 도 그대로 (단, **창 위치는 0,0 으로 남았다** — 원복 코드 보완 필요) |
| **부모(우리 판)를 파괴하면** | ❌ **한글이 죽는다.** 창 핸들은 3초 넘게 살아 있는 척하다가, 이후 COM 호출이 6초 매달린 뒤 `RPC 실패`. 프로세스는 사라졌고 **저장하겠냐고 묻지도 않았다** |

### '부모 파괴'가 무슨 말인가

윈도우에서 창은 부모-자식 관계를 맺는다. 임베드란 한글 창을 우리 판의
**자식 창으로 만드는 것**이고, 윈도우 규칙상 **부모 창이 사라지면 자식 창은
반드시 함께 파괴된다.** 여기서 '부모 파괴'는 우리 앱 창이 없어지는 모든 경우다
— 정상 종료, 창 X, 예외로 죽음, 작업관리자 강제 종료, 윈도우 재시작.

### "부모를 안 죽이면 되지 않나" — 두 갈래로 갈린다 (2026-07-29 추가 실측)

| 죽는 방식 | 결과 |
|---|---|
| 정상 종료 (닫기 전에 `SetParent` 로 떼어낸다) | ✅ 문제 없다. 위 표의 '정상 해제' |
| 우리 판만 파괴 (레이아웃 재구성 등, 앱은 살아 있음) | ❌ 한글 프로세스가 죽는다 |
| **파이썬을 강제 종료** (`taskkill /F` — 정리 코드가 한 줄도 못 돈다) | ❌ 프로세스는 살아남지만 **문서 창이 파괴돼 유령이 된다** |

세 번째가 이번에 새로 잰 것이다. 강제 종료 12초 뒤에도 `Hwp.exe` 는 메모리에
남아 있었지만, 창 목록을 뒤져 보니 **문서 창이 통째로 없다** — 화면에 아무것도
없고, 끌 방법도 없고, 저장 안 된 원고를 안은 채 계속 떠 있는 프로세스만 남는다.
작업관리자로 죽이는 것 말고는 손쓸 데가 없다.

즉 "부모를 파괴하지 않으면 된다"는 **우리가 코드를 돌릴 수 있을 때만** 성립한다.
크래시·강제 종료·윈도우 업데이트 재시작처럼 우리 정리 코드가 못 도는 죽음에서는
성립하지 않는다.

마지막 줄이 결론을 정한다. 우리 앱이 닫히거나(정상 종료 순서를 놓치거나)
예외로 죽으면, 선생님이 고치고 있던 문서가 **저장 프롬프트도 없이** 함께
사라진다. 이 도구의 존재 이유가 "문항 작업 중 원문 소실 방지"인데
(`docs/NEXT.md` 의 변환 롤백 작업이 그 이야기다), 그 반대편에 새 소실 경로를
하나 만드는 셈이다.

## 아직 못 잰 것 (사람 눈이 필요)

`python spikes\embed_spike.py` 로 창을 띄워 ①②를 누른 뒤 직접 확인:

1. **IME** — 임베드된 한글에 한글 타자·한자 변환이 정상인가
2. **입력 큐 결합** — 한글에서 파일>열기 같은 대화상자를 띄웠을 때 좌상단
   '심장박동' 숫자가 멈추는가 (멈추면 한글이 멎을 때 우리도 멎는다)
3. **4K/배율** — 자식 창이 된 뒤 글씨가 깨지거나 흐려지는가

②까지만 눌러 보고 ⑤(파괴 시험)는 누르지 않아도 된다 — 이미 결과를 알고 있다.

## 판단

**도킹을 유지한다.** 임베드는 "창 하나가 더 뜬 느낌"을 없애 주지만, 대가가
문서 소실 위험이다. 얻는 게 미관이고 잃는 게 원고라면 바꿀 이유가 없다.

도킹이 지금 주는 어색함(창 테두리·제목줄이 판 위에 보이는 것)은 임베드까지
가지 않고도 줄일 수 있다:

- 도킹 중에만 한글 제목줄·테두리를 떼기 (`WS_CAPTION` 제거, 부모는 그대로) —
  SetParent 를 안 쓰므로 파괴 연쇄가 없다. **다음에 시험해 볼 값어치가 있다.**
- 소유자(owner) 로 묶어 z-order·최소화를 함께 움직이기 — 단, 소유자 파괴도
  소유된 창을 파괴하므로 위험은 임베드와 같다. 권하지 않는다.

## 뒤집힌 판단 — 임베드를 기본으로 넣었다 (2026-07-30)

사용자 결정: "도킹을 다듬고 있지만 결국 임베드를 포기할 수가 없다. 한번
써 보고 나중에 결정하겠다." 위험을 아는 채로 **직접 써 보고 고르기** 위해
임베드를 제품에 넣었다 (`hwp_palette/hwp/hwp_embed.py`).

- 기본값은 임베드다. 도구줄의 `⇄ 임베드/도킹` 단추로 그 자리에서 갈아탄다
  (설정 `dock_mode`). 도킹 코드는 그대로 남아 있다.
- 문서 소실을 막으려고 **떼어내기를 세 겹**으로 건다: 정상 해제(`stop`),
  인터프리터 종료(`atexit`), 판이 파괴되려 할 때(`<Destroy>` 바인딩).
- 세 겹으로도 못 막는 것은 여전히 **작업관리자 강제 종료** 하나다.
  그때는 문서 창이 사라지고 `Hwp.exe` 만 유령으로 남는다.

제품 코드로 재확인한 것 (`spikes/embed_module_check.py`, 자동):
SetParent ✓ / 창 키우면 자식도 따라 커짐 ✓ / 임베드 중 COM 0.20초 ✓ /
해제 뒤 제목줄·스타일 원복 ✓ / 해제 뒤 COM 생존 ✓.

## 남긴 것

- `spikes/embed_spike.py` — 일회용 실험 도구. 제품에 포함되지 않는다.
  다시 재고 싶을 때 그대로 돌리면 된다.
